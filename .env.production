# Backend Environment Variables
    DATABASE_URL=postgres://user:password@db:5432/myjourney_db
    JWT_SECRET=your_strong_jwt_secret_here # IMPORTANT: Change this to a strong, unique secret!
    BACKEND_INTERNAL_URL=http://backend:3001
    BACKEND_EXTERNAL_URL=http://localhost:3001 # Change this to your server's public IP/domain if not running locally

    # Frontend Environment Variables
    VITE_API_BASE_URL=http://localhost:3001 # Change this to your server's public IP/domain if not running locally